{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "air_flow_paths.schema.json",
  "title": "Air Flow Paths",
  "description": "HVAC zone definitions, cross-zone links, and adjacency graph for CONTAM-style multi-zone aerosol transport.",
  "type": "object",
  "required": ["platform"],
  "properties": {
    "platform": {"type": "string"},
    "description": {"type": "string"},
    "oa_fraction": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Default outdoor-air fraction for Contam-aligned HVAC recirculation (native twin). Contam OAFrac default is 0.2."
    },
    "hvac_duty": {
      "type": "number",
      "minimum": 0,
      "description": "Default AHU duty multiplier on supply total (1.0 = design Fahs). Set 0.5 to match Contam hobbyist night/steady ContamX frames."
    },
    "hvac_zones": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/HVACZone"
      }
    },
    "cross_zone_links": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/CrossZoneLink"
      }
    },
    "adjacency": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/AdjacencyEdge"
      }
    }
  },
  "$defs": {
    "HVACZone": {
      "type": "object",
      "required": ["id", "rooms"],
      "properties": {
        "id": {"type": "string"},
        "rooms": {
          "type": "array",
          "items": {"type": "string"},
          "description": "Room IDs that MUST exist in spatial_layout.json"
        },
        "ach": {
          "type": "number",
          "minimum": 0,
          "description": "Air changes per hour for this HVAC zone"
        },
        "oa_fraction": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Outdoor-air fraction for Contam-aligned recirculation (overrides top-level)"
        },
        "hvac_duty": {
          "type": "number",
          "minimum": 0,
          "description": "AHU duty multiplier on supply/return (1.0 = design; Contam hobbyist night ≈ 0.5)"
        },
        "description": {"type": "string"}
      }
    },
    "CrossZoneLink": {
      "type": "object",
      "required": ["from", "to", "flow_rate_m3h"],
      "properties": {
        "from": {
          "type": "string",
          "description": "Source zone or HVAC zone ID"
        },
        "to": {
          "type": "string",
          "description": "Destination zone or HVAC zone ID"
        },
        "flow_rate_m3h": {
          "type": "number",
          "minimum": 0,
          "description": "Volumetric flow rate in m³/hour"
        },
        "is_hvac_ducted": {
          "type": "boolean",
          "description": "Whether airflow passes through HVAC filtration"
        },
        "path": {"type": "string"},
        "description": {"type": "string"}
      }
    },
    "AdjacencyEdge": {
      "type": "object",
      "required": ["from", "to"],
      "properties": {
        "from": {"type": "string"},
        "to": {"type": "string"},
        "type": {
          "type": "string",
          "description": "Adjacency link type (e.g. passageway, ladder_well, turbolift)"
        }
      }
    }
  }
}
