"""Crusher simulation REST service — HTTP job API over ShipSimulation.

Import :func:`create_app` from :mod:`api.app` (requires the ``api`` extra).
``api.jobs`` (the job manager) has no web-framework dependency.
"""
