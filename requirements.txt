# Flexible constraint file for local development.
# CI, Docker, and Sonar-safe installs use the hash-pinned lock instead:
#   requirements.lock.txt
# Regenerate after changing pyproject.toml dependencies:
#   uv lock
#   uv export --frozen --all-extras --no-emit-project -o requirements.lock.txt

# Core simulation dependencies
numpy>=1.24
pyyaml>=6.0

# Validation
pydantic>=2.0

# Dashboard (optional for CI — only needed to run dashboard.py)
streamlit>=1.28
plotly>=5.0
pandas>=2.0
pydeck>=0.8
Pillow>=10.0

# GIS spatial bridge (optional)
geopandas>=0.14
networkx>=3.0

# Naval blueprint import (optional PDF raster; images need Pillow above)
# pypdfium2>=4.0

# AWS campaign deploy / Batch helpers
boto3>=1.34

# Testing / CI tooling
pytest>=7.0
pytest-cov>=4.0
pytest-timeout>=2.3
ruff>=0.4
check-jsonschema>=0.28
