# Copy to deploy/aws/.env (gitignored) or export in your shell.
# Never put real values in tracked files.

AWS_PROFILE=picard
AWS_REGION=us-east-1
ACCOUNT_ID=<ACCOUNT_ID>
BUCKET=<BUCKET>
CAMPAIGN_BUCKET=<BUCKET>
